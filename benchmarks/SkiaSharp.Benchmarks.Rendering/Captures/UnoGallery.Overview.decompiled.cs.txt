// Auto-generated by SkpDecompiler. Reproduces the drawing captured in an
// .skp file via SKCanvas calls. Indexed resources (paints, paths, images,
// text blobs) are emitted as {resourceTable}[N] placeholders — filling in the
// resource tables requires parsing the data buffer, which this decoder
// currently punts on.
using SkiaSharp;
using SkiaSharp.Tests.Visual;

namespace SkiaSharp.Benchmarks.Rendering.Scenes;

public sealed class DecompiledScene : ISkiaScene
{
	public string Name => "DecompiledScene";
	// Original cull: (-536870880, -536870880, 536870880, 536870880) — snapped to 1024x1024 for benchmarking.
	public SKImageInfo Info => new(1024, 1024, SKColorType.Rgba8888, SKAlphaType.Premul);

	// Resolve these from the .skp's data-buffer section (out of scope for the
	// current decoder). For a round-trippable scene, replace with concrete
	// SKPaint / SKPath / SKImage / SKTextBlob instances.
	private readonly SKPaint[] paintTable = System.Array.Empty<SKPaint>();
	private readonly SKPath[] pathTable = System.Array.Empty<SKPath>();
	private readonly SKImage[] imageTable = System.Array.Empty<SKImage>();
	private readonly SKTextBlob[] textBlobTable = System.Array.Empty<SKTextBlob>();

	public void Draw(SKCanvas canvas)
	{
		canvas.DrawPaint(paintTable[1]);
		canvas.Save();
		// SET_M44 (SkM44, column-major):
		canvas.SetMatrix(new SKMatrix44 {
			/* col 0 */ 1, 0, 0, 0,
			/* col 1 */ 0, 1, 0, 0,
			/* col 2 */ 0, 0, 1, 0,
			/* col 3 */ 0, 0, 0, 1,
		});
		// DRAW_PICTURE picIdx=1 — nested SKPicture; not resolved
		// DRAW_PICTURE picIdx=2 — nested SKPicture; not resolved
		// DRAW_PICTURE picIdx=3 — nested SKPicture; not resolved
		canvas.Save();
		// SET_M44 (SkM44, column-major):
		canvas.SetMatrix(new SKMatrix44 {
			/* col 0 */ 1, 0, 0, 0,
			/* col 1 */ 0, 1, 0, 0,
			/* col 2 */ 0, 0, 1, 0,
			/* col 3 */ 48, 24, 0, 1,
		});
		canvas.DrawText(textBlobTable[1], 0f, 0f, paintTable[2]);
		canvas.Restore();
		canvas.Save();
		// SET_M44 (SkM44, column-major):
		canvas.SetMatrix(new SKMatrix44 {
			/* col 0 */ 1, 0, 0, 0,
			/* col 1 */ 0, 1, 0, 0,
			/* col 2 */ 0, 0, 1, 0,
			/* col 3 */ 0, 68, 0, 1,
		});
		// DRAW_PICTURE picIdx=4 — nested SKPicture; not resolved
		// DRAW_PICTURE picIdx=5 — nested SKPicture; not resolved
		canvas.Save();
		canvas.ClipRect(new SKRect(0f, 0f, 1024f, 572f), /* op */ SKClipOperation.Intersect, antialias: false);
